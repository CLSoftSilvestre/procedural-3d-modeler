/** Drag-and-drop MIME type carrying a NodeDef.type from the palette to the graph canvas. */
export const DND_NODE_MIME = 'application/p3m-node';

/** Drag-and-drop MIME carrying a saved project's id (inserted as an assembly component). */
export const DND_COMPONENT_MIME = 'application/p3m-component';
